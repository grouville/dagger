# Deliberately matches the fixture compiler but requests one extra component.
# This tests component preservation across edits, not a faster compiler.
[toolchain]
channel = "1.97.1"
profile = "minimal"
components = ["rustfmt"]
